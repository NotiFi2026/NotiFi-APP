/**
 * 노인 본인 홈 — 라우트 파일은 조합만 한다.
 */

import { RecipientHomeView } from '@/features/recipient/presentation/components/RecipientHomeView';

export default function RecipientHomeScreen() {
  return <RecipientHomeView />;
}
