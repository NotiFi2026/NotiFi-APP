/**
 * B-2 노인 등록 — 라우트 파일은 조합만 한다.
 */

import { RegisterView } from '@/features/careTargets/presentation/components/RegisterView';

export default function CareTargetRegisterScreen() {
  return <RegisterView />;
}
