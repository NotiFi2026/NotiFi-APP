/**
 * A5 노인 연결코드 — 라우트 파일은 조합만 한다.
 */

import { RecipientCodeView } from '@/features/auth/presentation/components/RecipientCodeView';

export default function RecipientCodeScreen() {
  return <RecipientCodeView />;
}
